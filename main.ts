let model: string= "civic rebirth";
let engine: string= "1.8cc";
console.log(`-My car is ${model}. 
-its engine capacity is ${engine}.`);  //applying features of template literals
                                       //print values of variables in string
                                       //started next line in same string text 